# Korean Romanization Keyboard — 系統規劃

> 版本 2　2026-08-13
> 本文取代第一版規劃。差異集中在第 3 節的四個設計決策。

---

## 1. 專案定位

**一句話**：讓不會韓文鍵盤的人，用英文 QWERTY 鍵盤打出完整的韓文句子。

```
annyeonghaseyo jeoneun hagsaengibnida
        ↓
    안녕하세요 저는 학생입니다
```

目標使用者是正在學韓文、只熟悉 QWERTY、不想記두벌식鍵盤位置的外國人。

這不是羅馬拼音查詢器，也不是翻譯器。它是輸入法。判準是：**沒收錄在任何詞表裡的人名、地名、新詞，一樣要打得出來。**

### 與두벌식的差異

두벌식要求使用者記住字母在鍵盤上的位置：

```
dkssud → 안녕     （d=ㅇ  k=ㅏ  s=ㄴ）
```

本專案讓使用者用已經會的羅馬拼音：

```
annyeong → 안녕
```

---

## 2. 系統架構

```
英文輸入
   ↓
字母對照表（Latin → Jamo）
   ↓
Parser：列出所有合法切法（候選格）
   ↓
排序規則：挑出第一名
   ↓
Hangul 組字器（Jamo → 音節）
   ↓
韓文輸出（組字中可用 ↓ 換候選）
```

四個元件全部是規則，不含模型、不含訓練、不含後端。

Parser 與組字器必須解耦。日後要換羅馬化規則時，Unicode 組字那一段不應該被動到。

---

## 3. 四個關鍵設計決策

這一節記錄的是「為什麼」，比規格本身更重要。日後想改架構時先讀這裡。

### 3.1 採用「轉寫式」而非「轉錄式」羅馬化

**決策**：使用者輸入的是韓文的**拼寫**，不是**發音**。

韓國官方的 Revised Romanization 有兩套。一般用的 transcription 轉寫的是發音，會套用連音、同化、口蓋音化、ㅎ 脫落等音韻規則，過程中發生資訊喪失，**反推不回原本的字**。

實測（純 parser 加組字器，無詞表）：

| 發音式輸入 | 期望 | 實際產出 |
|---|---|---|
| gamsahamnida | 감사합니다 | 감사**함**니다 ✗ |
| hagsaengimnida | 학생입니다 | 학생**임**니다 ✗ |
| oneureun | 오늘은 | 오느른 ✗ |
| joayo | 좋아요 | 조아요 ✗ |
| mani | 많이 | 마니 ✗ |
| gachi | 같이 | 가치 ✗ |

規律：**凡是收音後面接母音開頭的音節，反推就會錯。** 這涵蓋韓文極大比例的動詞變化。

改用 transliteration 之後，這些全部確定且可逆：

```
gamsahabnida → 감사합니다
hagsaengibnida → 학생입니다
oneuleun → 오늘은
johayo → 좋아요
manhi → 많이
gati → 같이
```

代價是使用者要照拼寫打，不能照唸的打。對學習者而言這反而是特色：這套輸入法會逼你記住正確拼寫。

### 3.2 不使用機器學習

**決策**：v1 與 v2 都不訓練模型。

理由：

1. 這是有限問題。韓文音節共 11,172 個（19 × 21 × 28），可完整列舉。唯一的未知數是切分，而有限詞彙上的切分靠查表加次數統計就能解決。
2. 更難的題目已證明統計方法夠用。日文的假名轉漢字比這題難得多，ATOK、Mozc 幾十年來的核心就是詞典加統計語言模型。
3. 報酬遞減。預設規則 82%、加詞頻表約 95%、加音節統計約 97%、加神經網路可能 98%。最後一兩個百分點的代價是訓練管線、5 至 10 MB 下載量、失去確定性、失去可修補性。單人專案不划算。

**重新考慮的觸發條件**：上線後若使用者測試顯示失敗案例裡超過三成是拼法變體造成（而非切分錯誤），且等價規則層已停在 90% 以下爬不上去。

### 3.3 候選用鍵盤選字，不用滑鼠點擊

**決策**：像中文輸入法一樣，↓ 鍵在候選之間移動，空白鍵確定。

這代表 v1 就必須實作組字狀態（composition state），因為候選操作只在「這個詞尚未確定」時才有意義。

滑鼠點擊修正保留，但排第二順位。兩者服務不同時機：

- **↓ 選字**：打字當下，詞還在組字狀態
- **點擊**：打完三個詞之後才發現前面錯了

### 3.4 桌機優先，手機排 v2

理由：

1. 手機上使用者裝韓文鍵盤只要 20 秒，價值主張最弱。
2. 網頁在手機上做 IME 級組字（composition event、自動修正干擾、inputmode）是另一個工程等級。
3. ↓ 鍵在手機上不存在，選字機制無法沿用。

v1 仍需 responsive 排版讓手機看得下去，但不保證手機輸入體驗。

---

## 4. 元件規格

### 4.1 字母對照表

以官方 RR transliteration 為基礎，索引順序即 Unicode 順序。

**初聲（19，index 0–18）**

| # | Jamo | Latin | # | Jamo | Latin | # | Jamo | Latin |
|---|---|---|---|---|---|---|---|---|
| 0 | ㄱ | g | 7 | ㅂ | b | 14 | ㅊ | ch |
| 1 | ㄲ | kk | 8 | ㅃ | pp | 15 | ㅋ | k |
| 2 | ㄴ | n | 9 | ㅅ | s | 16 | ㅌ | t |
| 3 | ㄷ | d | 10 | ㅆ | ss | 17 | ㅍ | p |
| 4 | ㄸ | tt | 11 | ㅇ | *（空）* | 18 | ㅎ | h |
| 5 | ㄹ | l | 12 | ㅈ | j | | | |
| 6 | ㅁ | m | 13 | ㅉ | jj | | | |

**中聲（21，index 0–20）**

| # | Jamo | Latin | # | Jamo | Latin | # | Jamo | Latin |
|---|---|---|---|---|---|---|---|---|
| 0 | ㅏ | a | 7 | ㅖ | ye | 14 | ㅝ | wo |
| 1 | ㅐ | ae | 8 | ㅗ | o | 15 | ㅞ | we |
| 2 | ㅑ | ya | 9 | ㅘ | wa | 16 | ㅟ | wi |
| 3 | ㅒ | yae | 10 | ㅙ | wae | 17 | ㅠ | yu |
| 4 | ㅓ | eo | 11 | ㅚ | oe | 18 | ㅡ | eu |
| 5 | ㅔ | e | 12 | ㅛ | yo | 19 | ㅢ | ui |
| 6 | ㅕ | yeo | 13 | ㅜ | u | 20 | ㅣ | i |

**終聲（28，index 0–27）**

| # | Jamo | Latin | # | Jamo | Latin | # | Jamo | Latin | # | Jamo | Latin |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 0 | *（無）* | *（空）* | 7 | ㄷ | d | 14 | ㄿ | lp | 21 | ㅇ | ng |
| 1 | ㄱ | g | 8 | ㄹ | l | 15 | ㅀ | lh | 22 | ㅈ | j |
| 2 | ㄲ | kk | 9 | ㄺ | lg | 16 | ㅁ | m | 23 | ㅊ | ch |
| 3 | ㄳ | gs | 10 | ㄻ | lm | 17 | ㅂ | b | 24 | ㅋ | k |
| 4 | ㄴ | n | 11 | ㄼ | lb | 18 | ㅄ | bs | 25 | ㅌ | t |
| 5 | ㄵ | nj | 12 | ㄽ | ls | 19 | ㅅ | s | 26 | ㅍ | p |
| 6 | ㄶ | nh | 13 | ㄾ | lt | 20 | ㅆ | ss | 27 | ㅎ | h |

**容錯規則（v1 唯一的一條）**

輸入的 `r` 與 `l` 一律都收成 ㄹ，不分位置。`sarang` 與 `salang` 都得到 사랑。成本是零，但省掉學習者最常見的一種挫折。

### 4.2 Hangul 組字器

整個組字邏輯是一行算術：

```
codepoint = 0xAC00 + 初聲index × 588 + 中聲index × 28 + 終聲index
```

反向分解：

```
offset = codepoint − 0xAC00
初聲 = offset ÷ 588
中聲 = (offset % 588) ÷ 28
終聲 = offset % 28
```

有效範圍 0xAC00–0xD7A3，共 11,172 個音節。

這一段寫完就永遠正確，附上全域對拍測試（見 6.2）之後不用再回頭看。

### 4.3 Parser

輸入一段沒有分隔的羅馬字，輸出所有合法的音節序列。

實作方式是位置索引的動態規劃：對每個起點嘗試所有可能的（初聲 + 中聲 + 終聲）組合，遞迴解析剩餘部分，記憶化。單一音節的羅馬字最長 7 個字元（例：`kkwaeng` = 꽹）。

Parser 必須回傳**全部候選**，不是單一結果。挑選是下一層的事。

實測歧義程度（50 個常用詞）：平均每個詞有 3.9 種合法解，只有極少數詞是唯一解。但其他候選幾乎都不是真正的韓文：

```
서울     → 세오울
사랑해   → 살앙해、사랑하에
안녕하세요 → 안녜옹하세요、안녕핫에요
맛있어요  → 마시쎄오요、마싯세오요
```

### 4.4 排序規則

實測四種確定性規則在 50 個常用詞上的表現：

| 規則 | 正確 |
|---|---|
| 母音長優先 + 收音多優先 | 21 / 50 |
| **母音長優先 + 收音少優先** | **41 / 50（82%）** |
| 母音短優先 + 收音多優先 | 9 / 50 |
| 母音短優先 + 收音少優先 | 20 / 50 |

**採用：母音盡量吃長，收音盡量不吃。**

具體實作是解析時的嘗試順序：

1. 初聲：長的優先（`ch` 先於 `c`、`kk` 先於 `k`）
2. 中聲：長的優先（`yeo` 先於 `ye` 先於 `e`）
3. 終聲：先試「無終聲」，失敗才依長度由短到長遞增

第一個成功走完全長的路徑即為第一名。

**已知會答錯的常見詞**（作為固定回歸測試）：

```
가을 → 개울        있어요 → 이써요      좋아요 → 조하요
많이 → 만히        같이 → 가티          괜찮아요 → 괜찬하요
맛있어요 → 마시써요  한국어 → 한구거      생일 → 샌길
```

這 9 個詞落在 117 個音節裡，換算下來使用者大約每 13 個音節需要介入一次。

### 4.5 InputState 與鍵位

正在輸入的詞維持在**未確定狀態**（底線標示），候選列即時更新。確定後的文字不可再被解析器動到。

```ts
interface InputState {
  committed: string      // 已確定的韓文
  composing: string      // 目前這個詞的羅馬字原文
  candidates: string[]   // 目前這個詞的候選（最多 3 個）
  selected: number       // 目前選中的候選索引
}
```

**鍵位表**

| 鍵 | 行為 |
|---|---|
| ↓ / ↑ | 在候選之間移動 |
| 空白 | 確定目前候選，插入空格，開始下一個詞 |
| Enter | 確定，不插空格 |
| 1 / 2 / 3 | 直接選（**僅在組字中攔截**，其餘時候正常輸入數字）|
| Esc | 收起候選列，保留羅馬字原文不轉換 |
| Backspace | 組字中：刪一個羅馬字母並重算<br>非組字中：刪一個韓文音節 |
| `-` | 硬邊界（見 4.6）|
| 標點與符號 | 視同確定，直接輸出 |

**實作注意**

- 組字中必須 `preventDefault()` 攔截 ↓ ↑，否則游標會跑掉。
- 使用者打的是 ASCII，瀏覽器原生 IME 不會介入，可完全接管 `keydown`。但若使用者的系統輸入法正好開著中日韓模式會出事，要偵測 `isComposing` 並提示切回英文。
- **候選最多 3 個。** 中文使用者掃 9 個候選只要 0.3 秒因為讀得懂；本專案的使用者是韓文初學者，讀 가을 / 개울 要好幾秒，列表越長越幫倒忙。
- **只在真的有歧義時才跳候選列。** 每個詞都彈一次會累死人。
- 延後定案：空白鍵按下之前，整個詞都保持可重新解析。`seoul` 單看有歧義，`seoule` 就沒有了。

**候選列必須標示切法**

```
gaeul
┌──────────────────┐
│ 1  가을   ga-eul │
│ 2  개울   gae-ul │
└──────────────────┘
```

右邊那一欄讓使用者不必讀懂韓文也能選對。中文輸入法不需要這個，本專案需要。

### 4.6 連字號

語意：**連字號標記一個強制的音節邊界。**

連字號兩側各自照一般的多音節貪婪排序取最佳解，不強制那一段只能有一個音節——連字號保證的只是「邊界切在這裡」，不是「這一段剛好一個音節」。

如果整個字逐一標滿每個邊界（`ga-eul`，兩段都剛好一個音節），效果等同完全確定的唯一解，這是最初設計時實測出來的用法：允許段內多個音節時，26 個測試詞裡有 16 個即使把連字號插滿仍然有歧義，因為歧義發生在音節**內部**的字母層（`seo` 可以是 서 也可以是 세 + 오、`ae` 可以是 ㅐ 也可以是 ㅏ + ㅔ），所以才需要「每段剛好一個音節」這條規則來保證那 16 個詞也能確定。

但如果只是想釘住某一個特定的邊界、其餘讓詞照樣多音節解析（`chijeubol-i`，只確保 bol／i 不會被切成 bo／li），連字號兩側一樣各自取最佳解，不會因為某一段有好幾個音節就直接判定失敗——早期實作誤把「每段剛好一個音節」套用到每一段，導致這種用法直接回傳空候選（[input-state/candidates.ts](packages/input-state/src/candidates.ts)，2026-08-27 修正）。

大寫字母視同連字號（`gaEul` 等於 `ga-eul`），讓使用者不用打符號。

連字號是**逃生口**，給人名、新詞、以及排序失敗時使用。不是主要的正確性來源，因為初學者根本看不出來 개울 不是他要的字。

---

## 5. 已知限制

v1 上線時要誠實寫在網站上：

1. **必須照拼寫打，不是照發音打。** `hapnida` 而非 `hamnida`。網站要有明顯的說明與對照範例。
2. **預設正確率約八成**（依 50 詞手測，真實數字待語料驗證）。剩下兩成靠 ↓ 選字補足。
3. **不支援發音式輸入。** 排 v2。
4. **手機輸入體驗不保證。** 排 v2。
5. **不做拼法容錯**（`annyong`、`anyoung` 之類）。排 v2。

---

## 6. 測試策略

**沒有測試集就是在瞎改。** 這一節的優先級高於 UI。

### 6.1 黃金測試集

三百到五百組 `(羅馬字, 正確韓文)`。產生方式：

```
韓文詞句 →（羅馬化函式）→ 羅馬字
              ↑ 這一組就是測試資料
```

所以**要先寫韓文轉羅馬字的函式**，它同時是測試集產生器。內容要涵蓋：

- 基本母音與複合母音
- 雙子音（ㄲㄸㅃㅆㅉ）
- 單收音與雙收音（ㄳㄵㄶㄺㄻㄼㅄ 等 11 個）
- 連續音節、空格、標點
- 英文混輸、數字
- 4.4 列出的 9 個已知難詞

### 6.2 全域對拍

一條很強的不變式，成本極低：

```
對全部 11,172 個音節：toHangul(toRoman(x)) === x
```

任何改動之後跑一次。這條過不了就是對照表或組字器壞了。

### 6.3 回歸測試

每次調整規則都跑一次黃金測試集，記錄正確率。只有數字上升才算改進。

---

## 7. UI 規格

首頁就是工具本身。使用者進站後不該先讀文章。

```
──────────────────────────────────────────────
Korean Romanization Keyboard
Type Korean using Roman letters.

┌────────────────────────────────────────────┐
│ annyeonghaseyo jeoneun hagsaengibnida      │
└────────────────────────────────────────────┘

안녕하세요 저는 학생입니다

[ Copy ]                            [ Clear ]
──────────────────────────────────────────────
How to Type Korean with Romanization

annyeonghaseyo  → 안녕하세요
gamsahabnida    → 감사합니다
salanghae       → 사랑해
──────────────────────────────────────────────
```

v1 必備：

- 輸入框與輸出區
- 候選列（浮在游標附近）
- Copy 與 Clear
- 拼寫規則的簡短說明與對照表
- 桌機排版；手機至少看得下去

v1 不做：登入、設定頁、歷史紀錄、深色模式。

---

## 8. 技術選型

```
框架      Astro 或 Vite + 靜態頁
語言      TypeScript
樣式      Tailwind CSS
核心      獨立的 TypeScript package（不依賴框架）
資料      TypeScript 常數
部署      Cloudflare Pages
版本控制  GitHub
分析      Cloudflare Web Analytics
```

**不用 Next.js。** 四頁的純前端工具用不到它，而且部署到 Cloudflare Pages 還要處理 adapter。Astro 或純靜態頁更輕，Core Web Vitals 更好看，對 SEO 有利。

**核心引擎務必寫成獨立 package**，日後換框架、包 Chrome 擴充功能都不用動它。

v1 完全是 client-side application，沒有後端、沒有資料庫。使用者輸入的內容不離開瀏覽器，這件事寫進網站文案：*Your text stays in your browser.*

---

## 9. SEO

### 目標關鍵字

務實的長尾優先：

```
romanization to hangul
type korean with english letters
korean keyboard without korean layout
korean romanization keyboard
romanized korean keyboard
```

「korean keyboard online」被 branah、gate2home、lexilogos 這類老站盤據，新網域短期打不進去，不當主要目標。

**刪掉韓文關鍵字**（한글 로마자 키보드、한국어 로마자 입력）。那些搜尋的是韓國人，他們不需要這個工具，搜尋意圖不符。

### Landing pages

v1 只做首頁 `/`。穩定後再逐步加：

```
/how-to-type-korean
/korean-keyboard-for-foreigners
/romanization-to-hangul
```

不要一開始大量產生低品質頁面。純工具頁在 Google 眼中內容很薄，每一頁都要有真材料（對照表、常見錯誤、可複製的例句）。

### 基礎設定

`<title>`、meta description、canonical、Open Graph、sitemap.xml、robots.txt、semantic HTML、Google Search Console、Analytics、隱私權政策頁（掛 Analytics 就需要，日後上 Chrome Web Store 也要）。

---

## 10. 開發階段

### Phase 0　準備

- 確認 RR transliteration 官方規則細節
- 實測既有競品：Google Input Tools 韓文 transliteration、Naver 로마자 입력、branah、lexilogos
- 確認差異點（多半是「開箱即用、不用登入、SEO 進得來」，而非技術）
- SEO 關鍵字搜尋量與競爭度
- 決定網域名稱

### Phase 1　引擎（不做 UI）

1. 字母對照表
2. Hangul 組字器
3. 韓文轉羅馬字函式
4. 黃金測試集（用 3 產生）
5. Parser
6. 排序規則
7. 跑測試，確認落在八成上下

### Phase 2　輸入狀態

InputState、鍵位、候選列、Backspace、連字號、延後定案。做完這一步才真正是輸入法。

### Phase 3　UI

輸入框、輸出區、候選列、Copy、Clear、說明區、responsive。

### Phase 4　SEO 與部署

```
GitHub → Cloudflare Pages → 自訂網域 → HTTPS
→ Google Search Console → 送出 sitemap
```

### Phase 5　真實使用者測試

找韓文初學者、不會두벌식的人，觀察：

1. 他們自然會怎麼拼？
2. 哪些拼法最常失敗？
3. ↓ 選字直覺嗎？三個候選夠嗎？
4. 「照拼寫打不是照發音打」這件事他們多久才懂？
5. Backspace 符合預期嗎？

這些資料比繼續猜規則有價值得多，也決定 v2 該做什麼。

---

## 11. 成本

```
Hosting / SSL / CDN / Analytics / Backend / DB      NT$0
Domain                                    NT$300～600 / 年
```

第一年控制在 NT$1,000 以內。真正的成本是時間，以及正確率不夠時使用者不會再回來。

---

## 12. v2 以後

依價值排序，每一項都要等 Phase 5 的資料再決定：

1. **詞頻表排序**。韓文維基百科 dump 免費（CC BY-SA），數出前三到五萬個詞的詞頻，整個詞在表裡就給大加分。這是查找表不是 ML，預期把 82% 拉到 95% 以上。約 300 KB。
2. **發音模板**。使用者可切換成照發音打（`hamnida` → 합니다）。這一層一定要詞表，沒有純規則版本。切換時要小心：初學者分不出 합니다 和 함니다，正是他們還沒學會的事。與其讓他們選，不如預設拼寫模板，偵測到疑似發音式輸入時浮一行「你是不是要打 합니다？」。
3. **點擊修正**。已確定的文字點下去重開候選。
4. **拼法容錯**。先試等價規則（`eo ≈ uh ≈ u`、`oo ≈ u`、收音位置 `k ≈ g`）當額外候選，幾十行程式。撞到天花板再談別的。
5. **音節接續統計**。
6. **手機輸入**。
7. **PWA**。
8. **Chrome 擴充功能**。要重新處理 content script、input element 偵測、權限、商店審查、隱私權政策。
9. **原生 OS 輸入法**。完全不同層級的工程。

---

## 13. 不做的事

v1 與 v2 都不碰：

```
機器學習 / 神經網路
後端伺服器
資料庫
登入與帳號
雲端同步
AI API
大型韓文詞典（作為核心轉換機制）
原生 App
```

**最重要的一條原則**：詞表只能用於候選排序、自動完成、歧義消解。核心輸入功能不能依賴它。沒有詞表的情況下，parser 加組字器仍然必須能把任何合法的羅馬字串轉成韓文。

---

## 14. 上線門檻

只有一條：

> **桌機上能打完一整句韓文，錯的地方按 ↓ 就能換掉。**

其餘都可以之後再說。
